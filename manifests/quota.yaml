apiVersion: v1
kind: ResourceQuota

metadata:
  name: web-quota
  namespace: web-app

spec:
  hard:
    pods: "4"

    requests.cpu: "500m"
    requests.memory: "512Mi"

    limits.cpu: "1"
    limits.memory: "1Gi"
